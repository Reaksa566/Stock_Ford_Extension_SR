// server/routes/reportRoutes.js
const express = require('express');
const router = express.Router();

// Placeholder route to prevent crash
router.get('/', (req, res) => res.json({ message: 'Report routes ready.' }));

module.exports = router;